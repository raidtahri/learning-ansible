[defaults]
inventory = inventory/dev/hosts.yaml
role_path = ./roles #avoid roles because it depends on the current directory
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml


